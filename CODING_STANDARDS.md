# Coding standards

Read by `/code-review` (the Standards axis) when a diff is reviewed, and by the agent writing the diff before it. *(grows)*

A rule enters this file once a review has found it by hand twice, with the commits that found it after it. Until then the standard is what the gate enforces (`docs/agents/gate.md`: ruff, mypy over the Engine and the toolkit-free modules, the test tiers) and what the ADRs decide (`docs/adr/`, ADR-0011 for layout and the engine/UI seam, ADR-0013 for Row conventions).

## Rules

_None yet._
