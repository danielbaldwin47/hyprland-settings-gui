# Sourced from inside `input { }`: the category stack carries across the file boundary,
# so this is `input:kb_variant`, and $mainMod is still in scope.
kb_variant = intl
bind = $mainMod, K, exec, from-a-sourced-file
